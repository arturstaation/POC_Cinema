"use client"
function TituloConteudo({ titulo, conteudo }) {
    return (
        <div>
            <h1>{titulo}</h1>
            <p>{conteudo}</p>
        </div>
    );
}

export default TituloConteudo;
